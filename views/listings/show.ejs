<% layout("/layouts/boilerplate") %>
<div class="row mt-3">
    <div class="col-8 offset-3">
        <h3>See post in details :</h3>
  </div>
    <div class="card col-6 offset-3 show-card">
        <img
            src="<%= listing.image %>"
            class="card-img-top show-img"
            alt="listing-image"
        />
        <div class="card-body">
            <p class="card-text">
                <b><%= listing.title%></b><br />
                <%= listing.description%> <br />
                &#8377;<%= listing.price.toLocaleString("en-IN") %> <br />
                <%= listing.location%> <br />
                <%= listing.country%> <br />
            </p>
        </div>
    </div>
    <br />
    <div class="btns">
        <a
            href="/listings/<%= listing._id %>/edit"
            class="btn btn-dark col-1 offset-3 edit-btn"
            >Edit</a
        >
        <form method="POST" action="/listings/<%= listing._id%>?_method=DELETE">
            <button class="btn btn-dark offset-4">Delete</button>
        </form>
    </div>
    <div class="col-8 offset-3">
    <form method="POST" action="/listings/<%= listing.id %>/reviews" novalidate class="needs-validation">
        <div class="form-group">
            <label for="rating">Rating</label>
            <input type="range" id="rating" class="form-control" min="1" max="5" name="review[rating]">
        </div>
        <div class="form-group">
            <label for="comment">Comments</label>
            <textarea id="comment" class="form-control" rows="10" cols="30" name="review[comment]" required></textarea>
            <div class="invalid-feedback">Please add a comment before submitting.</div>
             
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
    <hr>

<h4>All Reviews</h4>

<div class="row row-cols-1 row-cols-md-2 g-4 mb-4">
    <% listing.reviews.forEach(review => { %>
    <div class="col">
        <div class="card h-100 shadow-sm review-card">
            <div class="card-body">
                <h5 class="card-title">Guest Review</h5>
                <p class="card-text"><%= review.comment %></p>
                <span class="badge text-bg-warning"><%= review.rating %> stars</span>
                <form class="mb-3" action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE" method="POST">
                    <button class="btn btn-sm btn-dark">Delete</button>
            </form>
            </div>
        </div>
    </div>
    <% }); %>
</div>
</div>
</div>